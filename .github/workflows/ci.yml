# CI quality gates (plan §4, M0). Every job here mirrors a Makefile target: a green local `make check`
# — run with Docker and the smsc-sim image, since it fails rather than skips (CI=1) — means a green
# pipeline, except for the gates check leaves out (contracts-types, load-smoke, migrate, manifests).
name: CI

on:
  push:
    branches: [main]
  pull_request:

# A new push to a PR makes the previous run obsolete; cancel it rather than pay for both.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

env:
  # Keep the linter version identical to the Makefile's, or CI and laptops disagree about style.
  GOLANGCI_VERSION: v2.12.2
  OASDIFF_VERSION: v1.26.0

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          # go.mod is the single source of the Go version; never pin it a second time here.
          go-version-file: go.mod
          cache: true
      - name: golangci-lint
        # v9 is the action line that drives golangci-lint v2; v6 only ever spoke v1, and would
        # either reject the version below or install a v1 binary that cannot read a v2 config.
        uses: golangci/golangci-lint-action@v9
        with:
          version: ${{ env.GOLANGCI_VERSION }}
      - name: Verify formatting
        run: |
          # gofmt -l lists non-compliant files; the tree must be formatted, not just lintable.
          unformatted=$(gofmt -l .)
          if [ -n "$unformatted" ]; then
            echo "::error::These files are not gofmt-clean:"
            echo "$unformatted"
            exit 1
          fi
      - name: Verify go.mod is tidy
        run: |
          go mod tidy
          if ! git diff --exit-code -- go.mod go.sum; then
            echo "::error::go.mod/go.sum are not tidy — run 'make tidy' and commit the result."
            exit 1
          fi

  test:
    name: Test (race)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true
      # The M8 resilience tests bind a real SMSC simulator, and its image lives in no registry. Without
      # this step they do not fail — they SKIP, which reads exactly like passing, and ten of them went a
      # whole milestone without ever running (step-250b). internal/testutil/ciguard now turns such a skip
      # into a failure whenever CI is set, so removing this step turns the pipeline red instead of quiet.
      - name: Build the SMSC simulator image
        run: make smsc-sim
      # Redpanda ne démarre pas si le noyau ne peut plus lui allouer de contexte d'I/O asynchrone.
      # Seastar, son socle, en réserve au démarrage ; la limite par défaut du runner
      # (fs.aio-max-nr=65536) est déjà consommée par les ClickHouse, Postgres et autres Redpanda que
      # `go test ./...` fait tourner en parallèle. Le dernier servi se voit refuser son contexte,
      # seastar lève, libc++abi abandonne — ce qui se lit `container exited with code 139` côté
      # testcontainers et « general protection fault in libc » dans dmesg, deux symptômes qui ne
      # ressemblent en rien à la cause. Ni mémoire (oomkilled=false, 9,5 Go libres) ni disque (72 Go).
      #
      # Mesuré : sans ce relèvement, la suite échoue 3 fois sur 4 dès que les tests M8 ajoutent leurs
      # conteneurs ; avec, 4 passages verts.
      - name: Relever la limite noyau d'I/O asynchrone (Redpanda/seastar)
        run: sudo sysctl -w fs.aio-max-nr=1048576

      - name: go test -race
        run: go test -race -timeout 10m ./...

  # Exercises the load harness, NOT the gateway's throughput. It runs the k6 script twice against a
  # local stub — idle (must pass) then slowed past the latency budget (must fail) — so a harness whose
  # thresholds came unwired breaks the build instead of reporting a meaningless green.
  # The sustained (8 000 req/s) and peak (15 000 req/s) profiles never run here: a GitHub runner
  # cannot hold them, and failures there would be noise. They belong on real hardware, in step-201.
  load-smoke:
    name: Load harness (smoke)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true
      - name: Install k6
        # k6 is a native binary, outside go.mod by plan §1.3 — go install cannot provide it.
        run: |
          set -euo pipefail
          curl -fsSL "https://github.com/grafana/k6/releases/download/v${K6_VERSION}/k6-v${K6_VERSION}-linux-amd64.tar.gz" \
            -o /tmp/k6.tar.gz
          echo "${K6_SHA256}  /tmp/k6.tar.gz" | sha256sum -c -
          tar -xzf /tmp/k6.tar.gz -C /tmp
          sudo install "/tmp/k6-v${K6_VERSION}-linux-amd64/k6" /usr/local/bin/k6
          k6 version
        env:
          K6_VERSION: 2.1.0
          K6_SHA256: 295d961ebfca306f295f1133068dcd403a8171c87f387928f5f30b0fbcff858a
      - name: Thresholds must pass idle and fail under load
        run: make load-smoke

  vuln:
    name: Vulnerabilities
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true
      - name: govulncheck
        run: |
          go install golang.org/x/vuln/cmd/govulncheck@latest
          govulncheck ./...

  manifests:
    name: Manifests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true
      # The Kubernetes schema only. What this repo itself requires of the manifests — probes on the
      # ops port, the grace period, secrets by reference, the HPA ceiling — is asserted by
      # internal/deploy under the `test` job.
      - name: kubeconform
        run: make kubeconform manifests

  image:
    name: Image builds, runs non-root and refuses an incomplete production config
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      # The release configuration itself, before any build: a malformed dockers_v2 entry must not be
      # discovered during a release.
      - uses: goreleaser/goreleaser-action@v6
        with:
          version: "~> v2"
          args: check

      # ONE image, not twelve. This job proves the pattern works; that all twelve follow it is the
      # Go guard's job, and that one costs no Docker at all. content-key-svc is the one that matters:
      # ADR-0011 makes it the narrowest surface of the set, and it holds the KEK.
      - name: Build the image
        run: make image SVC=content-key-svc

      - name: Non-root, and no shell to escalate from
        run: |
          set -euo pipefail
          img=ghcr.io/martialanouman/go-gateway/content-key-svc:dev

          # Exactly what the kubelet reads for runAsNonRoot: a non-numeric user makes it refuse the pod.
          user=$(docker inspect -f '{{.Config.User}}' "$img")
          [ "$user" = "65532:65532" ] || { echo "::error::USER=$user, want 65532:65532"; exit 1; }

          entry=$(docker inspect -f '{{json .Config.Entrypoint}}' "$img")
          [ "$entry" = '["/app"]' ] || { echo "::error::entrypoint=$entry"; exit 1; }

          # Proof at the filesystem level, not through a `which` we could not run for lack of a shell.
          docker create --name probe "$img" >/dev/null
          if docker export probe | tar -t | grep -Eq '(^|/)(sh|bash|dash|ash|busybox)$'; then
            echo "::error::the image ships a shell"; docker rm probe; exit 1
          fi
          docker rm probe >/dev/null

      # The behaviour ENVIRONMENT=production guarantees, and that an image would only be seen to have
      # lost at deploy time. Only the first refusal is checked here: everything past the configuration
      # gate needs a reachable Postgres, which is not this job's business.
      - name: The image must refuse an incomplete production config
        run: |
          set -euo pipefail
          img=ghcr.io/martialanouman/go-gateway/content-key-svc:dev

          if out=$(docker run --rm -e ENVIRONMENT=production "$img" 2>&1); then
            echo "::error::started on an incomplete production config"; echo "$out"; exit 1
          fi
          grep -q "POSTGRES_URL" <<<"$out" || { echo "::error::unexpected refusal: $out"; exit 1; }

  contracts:
    name: Contracts version gate
    runs-on: ubuntu-latest
    # Rien à comparer sur un push direct : la garde vit sur la PR, là où le bump peut encore être
    # corrigé avant qu'une version parte au registre.
    if: github.event_name == 'pull_request'
    steps:
      - uses: actions/checkout@v4
        with:
          # Le script compare HEAD à la branche de base ; il lui faut les refs distantes.
          fetch-depth: 0
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true
      - name: Install oasdiff
        run: go install github.com/oasdiff/oasdiff@${{ env.OASDIFF_VERSION }}
      - name: Contract change implies a version bump
        run: scripts/check-contracts.sh
        env:
          BASE_REF: origin/${{ github.base_ref }}

  contracts-types:
    name: Contracts generate TypeScript
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: api
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: api/package-lock.json
      # Prouve que les contrats se génèrent en TypeScript valide AVANT publication — sinon la
      # rupture n'apparaîtrait que dans le dépôt du tableau de bord.
      - name: Generate and typecheck
        run: |
          npm ci
          npm run build
          npm run typecheck

  migrate:
    name: Migrations apply and reverse
    runs-on: ubuntu-latest
    services:
      postgres:
        # PostgreSQL 18: the schema needs the native uuidv7() generator.
        image: postgres:18-alpine
        env:
          POSTGRES_USER: gateway
          POSTGRES_PASSWORD: gateway
          POSTGRES_DB: gateway
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U gateway -d gateway"
          --health-interval 5s
          --health-timeout 3s
          --health-retries 10
    env:
      POSTGRES_URL: postgres://gateway:gateway@localhost:5432/gateway?sslmode=disable
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true
      # Proves the acceptance criterion rather than asserting it: the schema applies, and its
      # down migration reverses it cleanly. A down that does not reverse is found here, not
      # during an incident.
      - name: migrate up
        run: go run ./cmd/migrate -dir migrations up
      - name: migrate down
        run: go run ./cmd/migrate -dir migrations down
      - name: migrate up again
        run: go run ./cmd/migrate -dir migrations up
